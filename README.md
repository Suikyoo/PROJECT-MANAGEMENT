HOW TO RUN:

# in /frontend: 

`npm install && npm run build && npm run serve`

***or simply***
`npm run super-start`

# in /backend:

## navigate to /database-server:
`docker compose up -d`

## navigate to /backend-server:
`npm install && npm run db:generate && npm run db:push && npm run seed && npm run start`


**or simply**
`npm run super-start`


# navigate to /login in frontend:

## credentials:
- username: francis
- password: password123




